#include<stdio.h>

int main() {

	int value - 100;
	int update;
	
	if(value > 10)
		update = 100;
	else
		update = 1;
		
	update = (value > 10)? 10 : 1; // Ask if the value is greater than 10. If true, 10, else 1.
	// update = (value > 10)? true output : false output;
	//variable = condition? true value : false value;
		

	return 0;
}
